This app is part of the [Hermex](../../README.md) monorepo — see the root
README for full setup instructions.

To run just this app:

```bash
pnpm dev:web        # from the repo root
```

or

```bash
pnpm dev             # from apps/web/
```
